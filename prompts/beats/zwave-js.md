# Z-Wave JS desk

Follow the Z-Wave JS organization as a device-protocol and ecosystem beat. Pay particular attention to newly supported devices, configuration parameters, device-specific compatibility fixes, interview/command-class behavior, controller reliability, and changes that will become visible in Home Assistant.

Treat generated device-config refreshes and routine maintenance as wire material unless they unlock a clearly evidenced group of devices. Never claim Home Assistant support has landed unless the required downstream work is also merged.
